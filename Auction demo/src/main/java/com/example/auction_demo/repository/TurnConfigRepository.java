package com.example.auction_demo.repository;

import com.example.auction_demo.entity.TurnConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TurnConfigRepository extends JpaRepository<TurnConfig,Long> {
}
